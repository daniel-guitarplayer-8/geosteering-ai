# numba-profiler MCP — Fase 1 §22.1 I1.10 (handlers reais)
# Instalação: pip install -r requirements.txt
# Pré-requisito: pip install -e . (do repo root) para geosteering_ai

mcp>=1.25.0,<2.0
numpy>=1.24
numba>=0.60
# geosteering_ai é importado via sys.path em runtime (ver server.py:_REPO_ROOT)
