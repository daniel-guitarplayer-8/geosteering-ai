# Sprint v2.43 — Rewrite Notebook JAX GPU Benchmark (`A-jax-gpu-benchmark-redesign`)

| Campo | Valor |
|:--|:--|
| **Versão** | v2.43 (Sprint A1.6) |
| **Código backlog** | `A-jax-gpu-benchmark-redesign` |
| **Data início** | 2026-05-21 |
| **Data merge** | 2026-05-22 |
| **Branch** | `feat/a16-jax-gpu-benchmark-redesign` |
| **Trilha** | A (Simulador) |
| **Esforço** | ~5h implementação + ~1h multi-agent review |
| **Modelo Claude** | Sonnet 4.6 |
| **Commits** | 4 (C00 + C01 + C02 review-fixes + C03) |
| **Resultado** | **DONE** — notebook reescrito, 8 bugs metodológicos + 9 review findings aplicados |

---

## Contexto

A Sprint A1 (`A-jax-gpu-validate`) fechou DONE-PARTIAL: paridade Fortran
<1e-12 confirmada em Colab T4 (163/163 PASS) mas gate de performance
reprovado em A/B/E (0.38×, 0.37×, 0.61× Numba). A auditoria
`v2.40.4_auditoria_resultados_sprint_a1` identificou 8 bugs metodológicos
no notebook (C1+C2 críticos, H1-H3 altos, M1-M3 médios) e duas
causas-raiz arquiteturais.

A Sprint A1.5 (v2.42, mergeada em main) implementou
`simulate_multi_jax_batched()` resolvendo as causas-raiz arquiteturais.

Esta sprint (A1.6) reescreve o notebook para consumir a API batched
e corrigir os 8 bugs metodológicos.

---

## Escopo Executado

### Mudanças (1 arquivo de notebook + 3 docs)

| Arquivo | Mudança | LOC |
|:--|:--|--:|
| `notebooks/colab_templates/validate_jax_gpu_v240.ipynb` | Rewrite completo (21 células: 10 md + 11 code) | ~580 (-509 antigas) |
| `docs/sprints/CURRENT.md` | Plan ativo durante sprint; esvaziado pós-merge | — |
| `docs/sprints/v2.43.md` | Snapshot imutável (este arquivo) | +novo |
| `docs/CHANGELOG.md` | Append `[v2.43]` no topo | +50 |
| `docs/ROADMAP.md` | `A-jax-gpu-benchmark-redesign` CANDIDATE → DONE | 1 linha |

### Zero mudança em código de produção

`simulate_multi_jax_batched` (v2.42) e `geosteering-cli benchmark`
consumidos como estão. Sem testes Python novos — notebook é deliverable
para o usuário executar em Colab T4.

---

## Mapeamento bug → fix (8 bugs do v2.40)

| Bug | Sev | Fix aplicado |
|:--|:-:|:--|
| **C1** Warmup global shape fixa | CRIT | Warmup **por-cenário** com batch size EXATO (`N_MODELS_PER_SCENARIO[s]`); SCENARIOS_JAX movido para Cell 3 |
| **C2** `statistics.median([T1..T5])` mascara cold-start | CRIT | Run 1 (cold) **separado** de `statistics.median([T2..T5])` (hot) |
| **H1** `d.platform == "gpu"` deprecada | HIGH | `jax.default_backend() in ("gpu", "cuda")` + check `"cuda" in str(d).lower()` |
| **H2** Sem `block_until_ready()` explícito | HIGH | Batched API já chama internamente; defensivo: ler `result.H_tensor.shape` após call |
| **H3** Warmup só `models[0]` | HIGH | Resolvido por design — `_UNIFIED_JIT_CACHE` chaveado `(n, npt)` |
| **M1** H dip 87.5° vs Numba 90° | MED | Documentado (validador JAX cap em 89° per `multi_forward.py:880`); tabela com `'(dip≠)'` no lugar de ratio; H fora do gate |
| **M2** `esp` alta variância | MED | Resolvido por design — batched cache não chaveia em `esp` |
| **M3** Numba baseline hardcoded i9-9980HK | MED | `measure_numba_baseline_t4()` via subprocess `geosteering-cli benchmark --workers W --threads T` |

---

## Multi-Agent Review Fan-out (C01 → C02)

3 agentes lançados em paralelo após C01 (rewrite inicial):

| Reviewer | Findings | Aplicáveis |
|:--|:--|:--|
| `feature-dev:code-reviewer` (sintaxe/CLI/edge cases) | 1 HIGH + 2 MED | HIGH-1 (FileNotFoundError), MED-1 (H annotation), MED-2 (import — REJEITADO) |
| `general-purpose` (JAX expertise) | 1 CRIT + 2 HIGH + 1 MED + 1 LOW | CRIT-2 (warmup batch size), HIGH-2 (timeout H), MED-4 (verificação subjacente) |
| `general-purpose` (static review) | 1 CRIT + 3 HIGH + 3 MED + 1 LOW | CRIT-1 (dimensional bug), HIGH-3 (raise H timeout), MED-3 (pytest_data dup), MED-4 (JAX_PLATFORMS), MED-5 (workers/threads), LOW-1 (M2 row) |

### C02 — Fixes Aplicados

| # | Severidade | Fix |
|:-:|:-:|:--|
| **CRIT-1** | CRIT | `latency_ms_per_model_hot = 3_600_000.0 / median_hot` (não `n_models / median_hot * 3.6e6` — off by `n_models`) |
| **CRIT-2** | CRIT | Warmup `np.tile(template, (N_MODELS_PER_SCENARIO[s], 1))` por cenário (XLA cache key inclui axis size de outer vmap) |
| **HIGH-1** | HIGH | `except (FileNotFoundError, OSError)` em `measure_numba_baseline_t4` |
| **HIGH-2** | HIGH | `NUMBA_CLI_TIMEOUT_S = {"H": 1800, default: 600}` (cenário H stress 8×8×8) |
| **MED-1** | MED | Tabela H imprime `'(dip≠)'` no lugar do ratio; legenda explica 87.5° vs 90° |
| **MED-3** | MED | Single `_pytest_report` load em Cell 9 reusado em Cells 18 + 20 |
| **MED-4** | MED | `JAX_PLATFORMS="cuda,cpu"` explícito (compat JAX 0.4.30+) |
| **MED-5** | MED | `NUMBA_BASELINE_WORKERS=4, NUMBA_BASELINE_THREADS=1` pinados em CLI subprocess |
| **LOW-1** | LOW | Linha M2 adicionada na tabela de Cell 0 |

### Rejected

- **MED-2** (import `_build_models` de `benchmark.py`): viola surgical changes — requer expor função privada no `__all__` da CLI, mudança não-essencial. Mantém copy com comentário canônico explícito ("qualquer mudança em CLI DEVE ser espelhada").

---

## Design Final do Notebook

```
§ Pré-amble
  Cell 0  (md):   Título + tabela 8 fixes + gate
  Cell 1  (code): JAX env vars [JAX_ENABLE_X64, JAX_PLATFORMS="cuda,cpu", JAX_COMPILATION_CACHE_DIR]

§ 1 — Configurações
  Cell 2  (md):   header
  Cell 3  (code): GIT_TAG, N_MODELS, SEED + SCENARIOS_JAX + N_MODELS_PER_SCENARIO + NUMBA_CLI_TIMEOUT_S

§ 2 — Setup
  Cell 4  (md):   header (H1 explicado)
  Cell 5  (code): drive.mount + git clone + pip install + GPU detect via default_backend()

§ 3 — Warmup batched
  Cell 6  (md):   explicação CRIT-2 (XLA cache key inclui n_models)
  Cell 7  (code): loop por SCENARIOS_JAX com batch size = N_MODELS_PER_SCENARIO[scen]

§ 4 — Paridade Fortran
  Cell 8  (md):   header
  Cell 9  (code): pytest -m gpu + single load _pytest_report
  Cell 10 (code): sanity check batched (shape, dtype, NaN/Inf)

§ 5 — Numba T4 LOCAL
  Cell 11 (md):   header (M3 + MED-5 explicados)
  Cell 12 (code): measure_numba_baseline_t4 + loop com pin workers/threads + timeout dinâmico

§ 6 — Benchmark JAX batched
  Cell 13 (md):   header (C1+C2+H2+M2 explicados)
  Cell 14 (code): _build_models + run_benchmark_batched_scenario (Run 1 cold + 4 hot, latency CRIT-1)

§ 7 — Tabela + Gate
  Cell 15 (md):   header
  Cell 16 (code): tabela 9-col (incl. C/H ratio) + gate ≥1.5× T4 LOCAL em A/B/E

§ 8 — Export
  Cell 17 (md):   header
  Cell 18 (code): JSON com benchmark_batched + numba_t4_local_baselines + fixes_applied + review_fixes_applied

§ 9 — Resumo
  Cell 19 (md):   header
  Cell 20 (code): print summary + próximos passos
```

---

## Métricas Reportadas pelo Notebook (esperadas em T4)

Cada cenário reporta:
- `run_1_cold_mod_h`: throughput Run 1 (deve ser ≈ hot se warmup efetivo)
- `hot_throughputs_mod_h`: 4 throughputs (Runs 2-5)
- `median_hot_mod_h`: `statistics.median` — métrica oficial
- `ratio_cold_to_hot`: cold/hot — esperado ≈ 1.0 (diagnóstico)
- `latency_ms_per_model_hot`: `3.6e6 / median_hot` ms (CRIT-1 fix)
- `numba_t4_local_mod_h`: subprocess `geosteering-cli benchmark` (M3 fix)
- `numba_baseline_historical_mod_h`: i9-9980HK (informativo)

**Gate**: `median_hot / numba_t4_local ≥ 1.5` em A, B, E. H excluído (M1).

---

## Decisões

| # | Decisão | Resolução |
|:--|:--|:--|
| D1 | Comparar vmap legacy vs batched no notebook? | **Não** — regression test em pytest T15 cobre paridade; notebook foca em batched only |
| D2 | `_build_models` import vs copy? | **Copy com comentário canônico** — surgical changes; MED-2 rejected |
| D3 | Cenário H entra no gate? | **Não** — dip mismatch 87.5° (JAX cap 89°) vs 90° (Numba CLI) torna ratio incomparável |
| D4 | GIT_TAG default? | `"main"` — usuário muda para `"v2.43"` após tag publicada (post-merge) |
| D5 | Pin workers/threads no CLI baseline? | **Sim** — `4w × 1t` para T4 n1-standard-4 (4 vCPUs sem HT); evita variância heurística |
| D6 | Cenário H timeout 1800s vs 600s? | **1800s** — H é stress test 8×8×8=512 combos × 10 modelos em 4 vCPUs |
| D7 | `JAX_PLATFORMS=""` vs `"cuda,cpu"`? | **`"cuda,cpu"` explícito** — compat JAX 0.4.30+ (empty string pode forçar CPU) |
| D8 | Move SCENARIOS_JAX de Cell 12 para Cell 3? | **Sim** — necessário para CRIT-2 (warmup em Cell 7 precisa de SCENARIOS_JAX + N_MODELS_PER_SCENARIO) |

---

## Karpathy Guidelines Aplicados

| Guideline | Aplicação |
|:--|:--|
| Think Before Coding | Plan mode + 1 plan agent + 3 review agents paralelos |
| Simplicity First | Sem dual-mode (vmap vs vmap_real); sem chunking; sem GUI; sem multi-GPU |
| Surgical Changes | 1 notebook + 3 docs; zero produção (`simulation/`, `cli/`). API batched já em v2.42 |
| Goal-Driven Execution | 8 bugs + 9 review findings com fix verificável; AST + content checks |
| No Speculative Features | Sem comparação legacy; sem `pmap`; sem complex64; gate apenas A/B/E |

---

## Itens Adiados / Fora de Escopo

- Execução real em Colab T4 → deliverable do usuário (Sprint A1.6 entrega o notebook;
  o gate só é confirmado quando usuário roda e reporta resultados)
- Dispatcher `simulation.simulate(backend=...)` → Sprint A2
- Geração de dataset on-the-fly JAX → Sprint A3
- Treinamento SurrogateNet → Sprint C (dep: A2+A3)
- `pmap` multi-GPU → fora do escopo (Colab T4 single-GPU)
- complex64 mixed precision → Sprint 13 / v1.7.0

---

## Próximos Passos Desbloqueados

| Sprint | Código backlog | Status |
|:--|:--|:-:|
| **Usuário executa em Colab Pro+ T4** | — | P1 (reporta tabela para Claude) |
| A2 | `A-jax-gpu-dispatcher` | P1 BACKLOG (dep: A1.5 ✓ + A1.6 ✓) |
| A3 | `A-jax-gpu-data-gen` | P1 BACKLOG (dep: A2) |
| C-surrogate-train | F4.3 SurrogateNet | P1 BLOCKED (dep: A1.5 ✓ + A1.6 ✓ + A2 + A3) |

---

## Como Usar (Para o Usuário)

```
1. Abrir Colab Pro+ com GPU T4 (Runtime → Change runtime type → GPU)
2. Upload notebooks/colab_templates/validate_jax_gpu_v240.ipynb
3. (Opcional) Mudar GIT_TAG="v2.43" se tag já publicada
4. Run All
5. Reportar JSON gerado em /content/drive/MyDrive/Geosteering_AI/sprint_a16/
   para nova sessão Claude (Sprint A2 ou análise de resultado)
```

---

*Snapshot imutável. Não editar após merge.*
