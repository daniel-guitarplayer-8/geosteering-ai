# Sprint v2.40 — MCP colab-bridge + tf.data + Mixed Precision

| Campo | Valor |
|:--|:--|
| **Versão** | v2.40 (tag `v2.40`, merge commit `0dab72b`) |
| **Data** | 2026-05-18 |
| **Branch** | `feature/v2.40-colab-bridge-mp16` |
| **Trilhas** | C (DL) + E (infra) + F (gov) |
| **Item de backlog** | I2.2 MCP colab-bridge + D5 mp16 fix + D6 tf.data flags |
| **Esforço** | 14-18h em 2-3 sessões |
| **Modelo Claude** | Opus 4.7 (1M context) |
| **Commits** | 11 (C01-C13 + hardening) |

## Contexto

Sprint v2.39 (API REST + Dockerfile.cpu) desbloqueou 3 frentes. Esta sprint
ataca 2 em paralelo:

1. **MCP colab-bridge (I2.2)** — automação de notebooks Colab via skill +
   hook + 3 templates
2. **Fix Mixed Precision order (D5)** — bug onde `set_global_policy()` era
   chamado DEPOIS de `build_model()`, deixando camadas em fp32
3. **tf.data parametrization (D6)** — 4 flags em PipelineConfig para
   tuning de shuffle/parallel/prefetch/cache

## Escopo Executado

### Novos arquivos (8)
- `.claude/commands/geosteering-colab-mcp.md`
- `.claude/hooks/colab-token-refresh.sh`
- `notebooks/colab_templates/{validate_jax_gpu_v240,train_v240_mp16,benchmark_tfdata_mp16}.ipynb`
- `notebooks/colab_templates/__README.md`
- `docs/perf_baselines/v2.40_tf_training_T4.json` (placeholder)
- `docs/reports/v2.40_colab_bridge_mp16_2026-05-18.md`

### Modificados (10)
- `geosteering_ai/config.py` (+4 flags D6)
- `geosteering_ai/data/pipeline.py` (parametriza build_tf_dataset)
- `geosteering_ai/training/loop.py` (setup_mixed_precision_policy módulo-level)
- `geosteering_ai/training/__init__.py` (export)
- `geosteering_ai/models/registry.py` (build_model_with_mp_policy helper)
- `tests/test_simulation_jax_*.py` (11 arquivos, +pytestmark gpu)
- `tests/conftest.py` (+skip gpu marker)
- `tests/test_training.py`, `tests/test_config.py`, `tests/test_data_pipeline.py`
- `pyproject.toml` (+marker gpu)
- `.claude/settings.json` (+hook colab-token-refresh)
- `configs/baseline.yaml` (+4 flags)
- `docs/PERFORMANCE_BASELINE.md` (+seção TF Training)
- `docs/CHANGELOG.md` + `docs/ROADMAP.md`

## Decisões D1-D11

| # | Decisão | Resolução |
|:--|:--|:--|
| D1 | Skill (não agente separado) | `geosteering-colab-mcp.md` Sonnet 4.6 effort=medium |
| D2 | Tier B (browser MCP) default, Tier C documentado | `mcp__colab-mcp__open_colab_browser_connection` |
| D3 | Automação JAX GPU | Híbrido: caminho (a) template + (c) marker `@pytest.mark.gpu` |
| D4 | Exposição API para Colab | ngrok HTTP tunnel (free 40 req/min ou $8 unlimited) |
| D5 | Fix mp16 order | Helper `build_model_with_mp_policy(config)` em registry.py |
| D6 | 4 flags tf.data (não 6) | shuffle/parallel/prefetch/cache_eval |
| D7 | Hook PreToolUse Bash matcher | Regex `\b(colab|gcloud)\b`, warn-only |
| D8 | 3 templates (não 8) | train_v240_mp16, validate_jax_gpu_v240, benchmark_tfdata_mp16 |
| D9 | Marker `gpu` em pytest | Skip automático se sem GPU física |
| D10 | PT-BR + D1-D14 + factories | Padrões obrigatórios mantidos |
| D11 | Benchmark v2.40 | 4 configs × 5 runs em T4+A100; gate mp16 ≥1.15× |

## Resultados

- **Suite**: 1653 PASS / 458 SKIPPED / 0 FAIL (após adicionar marker gpu, 109 testes JAX skipam em CPU)
- **Paridade Fortran**: <1e-12 PRESERVADA
- **Reviewers fan-out**: 5 findings ALTO/MED → 5 fixes aplicados em commit hardening (378e080)
- **Resposta D3**: SIM, automação JAX GPU viável via Colab Pro+

## Itens Adiados

Resolvidos posteriormente em v2.40.1 (patch) e v2.40.2 (refactor doc):
- 5 findings BAIXO dos reviewers → fechados em v2.40.1
- Inconsistência de documentação roadmap (v2.41 com 5 definições) → fechado em v2.40.2

## Próximos Passos Desbloqueados

Ver [../ROADMAP.md](../ROADMAP.md) §Backlog — itens com tag `unblocked-by:v2.40`.

---

*Snapshot imutável. Não editar após merge.*
