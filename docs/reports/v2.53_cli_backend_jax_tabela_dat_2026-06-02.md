# Relatório — Geosteering AI Simulation Manager v2.53

> **Gerado automaticamente** via `.claude/templates/report_template.md`
> **Data**: 2026-06-02
> **Branch**: `feat/jax-perf-o2-o3`
> **Escopo**: `geosteering-cli` — backend JAX GPU selecionável + tabela ASCII + salvar `.dat`/`.out` (conforme `geosteering-physics.md` §4) + recursos curados
> **Paridade Fortran**: **<1e-6 PRESERVADA** (10/10) — kernels NÃO tocados
> **Throughput**: **E n=200 = 233.692 mod/h** ≈ baseline 234.665 (−0,4% ruído) — **sem regressão**

---

## Resumo Executivo

A CLI `geosteering-cli` ganhou **escolha de backend** (`--backend numba` padrão CPU / `jax` GPU),
uma **tabela ASCII de resultados** (throughput, tempo, paralelismo, hardware detectado, NaN/Inf) e
**gravação `.dat`/`.out` 22-col** conforme `geosteering-physics.md` §4 (com `res_h`/`res_v` REAIS na
camada de cada `z_obs`, não zeros). O caminho **Numba é byte-a-byte o legado** (`simulate_multi(models=…)`
+ pool de workers → zero regressão de throughput); o **JAX** roteia pelo dispatcher parity-tested
`simulate_batch` (paridade medida `max|Δ| = 2,71e-14` vs Numba). Foram implementados 4 recursos curados
(`--json`, `--repeat N`, `--list-scenarios`, `--compare-backends`) e propostos outros 7 (seção 9). Corrigido
o bug do `--out` que salvava `repr(result)` em vez do tensor H.

---

## 1. Bugs Corrigidos (1)

| # | Bug | Root Cause | Arquivo(s) | Linha(s) | Fix Aplicado |
|:-:|:----|:-----------|:-----------|:--------:|:-------------|
| 1 | `simulate --out` salvava `repr(result)` no `.npz` (tensor perdido) | Lia `getattr(result, "H", None)` — o atributo do `MultiSimulationResultBatch` é `.H_stack` (sempre None → fallback `repr`) | `cli/simulate.py` | `_save_simulation_output` | Grava `np.savez(..., H=H6)` com o tensor real (`H_stack`); teste `test_npz_save_uses_real_tensor` guarda |

---

## 2. Features / Melhorias (8)

| # | Feature | Arquivo(s) | Impacto |
|:-:|:--------|:-----------|:--------|
| 1 | **`--backend {numba,jax}`** (padrão numba) + `--dtype` + `--jax-strategy` + `--warmup` | `cli/_main.py`, `cli/_backend.py`, `cli/_exec.py` | Usuário escolhe CPU/GPU; fallback gracioso jax→numba sem GPU |
| 2 | **Tabela ASCII de resultados** (Unicode box + fallback ASCII via env) | `cli/_table.py` | Throughput, tempo, workers×threads/strategy, hardware, NaN/Inf/finitude |
| 3 | **Detecção de hardware** (CPU/cores/RAM/GPU/VRAM/threads) defensiva | `cli/_hwinfo.py` | Probes `/proc`+`nvidia-smi`+`psutil`-opcional; nunca levanta |
| 4 | **Salvar `.dat`/`.out` 22-col §4** (`--format {npz,dat,none}`) com `res_h`/`res_v` reais | `cli/simulate.py`, `simulation/io/tensor_dat.py` | `.dat` re-legível por `load_binary_dat`; conforme `geosteering-physics.md` §4 |
| 5 | **`--json`** — saída machine-readable (apta a `\| jq`) | `cli/simulate.py`, `cli/benchmark.py` | Suprime a tabela; emite dict de stats + hardware |
| 6 | **`--repeat N`** — N rodadas hot → mediana + desvio do throughput | `cli/_exec.py`, handlers | Padrão de perf-gate (descarta cold-start) |
| 7 | **`--list-scenarios`** (benchmark) — lista A–H e sai | `cli/benchmark.py` | Descoberta sem simular |
| 8 | **`--compare-backends`** — numba × jax lado-a-lado (throughput, speedup, paridade `max\|Δ\|`) | `cli/_exec.py`, handlers | Guarda a fidelidade física entre backends |

---

## 3. Remoções / Deprecações (1)

| Item Removido | Motivo | Substituto |
|:--------------|:-------|:-----------|
| Lógica `.dat`/`.out` em `simulation/tests/sm_io.py` (corpo das funções) | Código de PRODUÇÃO (CLI) não deve importar de um subpacote `tests` | Relocado para `simulation/io/tensor_dat.py`; `sm_io.py` vira shim re-export (retrocompat 100%) |

---

## 4. Revisão de Código (`/geosteering-code-reviewer`)

**Findings**: 0 CRÍTICA · 0 ALTA · 2 MÉDIA · 2 BAIXA → **APROVAR**

| # | Severidade | Arquivo | Descrição | Ação |
|:-:|:----------:|:--------|:----------|:-----|
| 1 | — | (todos) | `import torch` / `globals().get` / errata física | ✅ 0 ocorrências |
| 2 | — | `cli/*` | `print()` em `geosteering_ai/` | ✅ Exceção D9 (stdout de CLI) — documentada nos docstrings |
| 3 | MÉDIA | `cli/simulate.py` | `.out` clássico (4 linhas) não codifica o eixo de TR | Documentado em comentário inline (multi-TR → use o `.dat` direto; 22-col é §4-conforme sempre) |
| 4 | MÉDIA | `cli/benchmark.py` | `handle_benchmark` longo (orquestrador) | Subdividido em `# ──` seções + `_list_scenarios`/`run_compare_backends` extraídos; aceitável |
| 5 | BAIXA | `cli/_table.py` | helpers `_fmt_int`/`_fmt_gb` com docstring de 1 linha (sem `Returns:`) | Triviais; aceitável (D5 permite brevidade em helpers privados) |
| 6 | BAIXA | `cli/_hwinfo.py` | `except Exception` largo em probes | Intencional (defensivo — probe nunca pode levantar); `# noqa: BLE001` |

**Proibições absolutas**: 0 `torch` · 0 `globals().get` · errata física intocada · 0 hardcoded paths.
**D1-D14**: 5/5 arquivos novos com mega-header D1 + `__all__` D8 + docstrings D5/D6 Google-style PT-BR.

---

## 5. Testes

### Suite

| Conjunto | OK | FAIL | Notas |
|:---------|:--:|:----:|:------|
| CLI + IO (mvp, backend_table, hwinfo, dat_save, warmup, sm_workers_io, simulation_io) | **92** | 0 | inclui 31 novos |
| Paridade Fortran (`test_simulation_compare_fortran`) | **10** | 0 | INVIOLÁVEL preservada |

### Novos Testes Adicionados (31)

| Arquivo | Testes | Checks-chave |
|:--------|:------:|:-------------|
| `tests/test_cli_backend_table.py` | 11 | resolve_backend (numba/jax/fallback/inválido); render_kv_table (Unicode/ASCII/truncamento/vazio); build_result_rows (numba vs jax/GPU/finitude) |
| `tests/test_cli_hwinfo.py` | 6 | chaves base/GPU; tipos; **nunca levanta** mesmo com `/proc`+subprocess sabotados |
| `tests/test_cli_dat_save.py` | 11 | helpers `_exec`; **conformidade §4** (col1=zobs, col2/3=res_h/res_v reais, col4/5=Hxx, col20/21=Hzz); fix `.npz`; **não-regressão de roteamento** (numba ≠ dispatcher) |
| `tests/test_cli_mvp.py` (estendido) | +3 | flags v2.53 em `--help`; `--list-scenarios` roda |

### Verificação manual end-to-end

```text
simulate --models 4 --backend numba         → tabela ASCII (Threadripper 7970X, 32/64, 251 GB) ✓
simulate --json                              → JSON limpo (apto a | jq) ✓
simulate --models 3 --out … --format dat     → .dat 48 rows; col2 res_h ∈ [5.34, 86.0] (NÃO zero) ✓
simulate --compare-backends                  → numba 6.312 / jax 2.639 mod/h; paridade max|Δ| = 2,71e-14 ✓
benchmark --scenario A --n 20                → "Cenário A — 5.025.919 mod/h" (grep) + tabela ✓
benchmark --scenario E --n 200 --backend numba → 233.692 mod/h ≈ baseline 234.665 ✓
benchmark --list-scenarios                   → A–H + combos/pos ✓
```

---

## 6. Arquivos Modificados

| Arquivo | Tipo | Descrição |
|:--------|:----:|:----------|
| `geosteering_ai/cli/_backend.py` | NEW | `resolve_backend(requested) → (backend, device)` + fallback jax→numba |
| `geosteering_ai/cli/_table.py` | NEW | `render_kv_table` (box Unicode/ASCII) + `build_result_rows` |
| `geosteering_ai/cli/_hwinfo.py` | NEW | `collect_hardware_info(*, want_gpu)` — probes defensivos |
| `geosteering_ai/cli/_exec.py` | NEW | `run_once`/`warmup_backend`/`finitude_stats`/`parity_max_abs_diff`/`models_to_batch`/`rho_at_obs_from_batch`/`run_compare_backends` |
| `geosteering_ai/simulation/io/tensor_dat.py` | NEW | Escritores `.dat`/`.out` 22-col (relocados de `tests/sm_io.py`) |
| `geosteering_ai/cli/_main.py` | MOD | Novos args (`--backend/--dtype/--jax-strategy/--warmup/--format/--json/--repeat/--compare-backends/--list-scenarios/--quiet`); version v2.53 |
| `geosteering_ai/cli/simulate.py` | MOD | Handler reescrito: backend branch + tabela + `--format` + features + fix `.npz` |
| `geosteering_ai/cli/benchmark.py` | MOD | Handler reescrito: backend branch + tabela + `_list_scenarios` + features (grep-line preservada) |
| `geosteering_ai/simulation/io/__init__.py` | MOD | Exporta `write_dat_from_tensor`/`write_out_file`/`compute_nmeds_per_angle` |
| `geosteering_ai/simulation/tests/sm_io.py` | MOD | Shim re-export de `io/tensor_dat` (retrocompat) |
| `tests/test_cli_{backend_table,hwinfo,dat_save}.py` | NEW | 28 testes |
| `tests/test_cli_mvp.py` | MOD | +3 testes (flags v2.53) + version v2.53 |

---

## 7. Integridade dos Simuladores

```bash
# Kernels de simulação NÃO tocados nesta sprint:
#   forward.py / _numba/* / _jax/kernel.py — intactos.
# A CLI REUSA simulate_multi (numba) e simulate_batch (dispatcher jax) sem alterá-los.
```

**Resultado**: ✅ **Paridade Fortran 10/10 (<1e-6)** preservada · **throughput E n=200 = 233.692 mod/h**
(≈ baseline 234.665) · **paridade numba×jax `max|Δ| = 2,71e-14`** (c128, < 1e-13). O ramo Numba é o
`simulate_multi(models=…)` legado byte-a-byte (pool de workers preservado — guarda
`test_run_once_numba_does_not_route_through_dispatcher`).

---

## 8. Notas de Migração / Breaking Changes

- **Nenhuma breaking change.** Defaults preservados: `simulate`/`benchmark` sem flags novas rodam
  como antes (backend numba, `--format npz`). A linha grep-able `Cenário X — Y mod/h` do benchmark
  é mantida.
- **`.dat` físico**: grava H **cru** (mesmo estado de decoupling do `.dat` do `tatu.x`); o decoupling
  (`ACp/ACx`) é etapa downstream do pipeline de dados, não da gravação.
- Importadores legados de `simulation/tests/sm_io.py` continuam funcionando (shim re-export).

---

## 9. Pesquisa — Recursos Propostos (não implementados) para `geosteering-cli`

Subconjunto curado **implementado** nesta sprint: `--json`, `--repeat N`, `--list-scenarios`,
`--compare-backends`. Demais recursos **propostos** (valor × risco):

| Recurso | Descrição | Valor | Risco |
|:--------|:----------|:-----:|:-----:|
| `--config YAML` | Carregar `SimulationConfig.from_yaml` (reprodutibilidade) | Alto | Baixo |
| `--device {cpu,gpu}` | Forçar device explicitamente (ortogonal a backend) | Médio | Baixo |
| `--strict-backend` | jax sem GPU → erro fatal (em vez de fallback) p/ CI GPU | Médio | Baixo |
| `--validate` | Gate de paridade numba×jax com tolerância (exit≠0 se exceder) | Alto | Médio |
| `--profile` | Timing por estágio (filtro Hankel / dipolos / reassembly) | Médio | Médio |
| `--dry-run` | Imprime a configuração resolvida + shapes sem simular | Baixo | Baixo |
| `--progress` | Barra de progresso por modelo (batches longos) | Baixo | Médio |

### Próximos Passos

1. Commit + push → CI (Python 3.13 CPU; jax[cpu]+numba) valida os 31 testes novos.
2. Validar o caminho **JAX em GPU real** (RTX A6000) — medir `--compare-backends` com `n_models ≥ 32`
   (crossover de ocupação) e `--backend jax` GPU end-to-end.
3. Avaliar promover `--config YAML` + `--validate` (maior valor/menor risco) na próxima sprint.

---

*Relatório gerado por Claude Code via template `.claude/templates/report_template.md`*
*Versão do template: 1.0.0 (2026-04-28)*
