# Auditoria de Ambiente — Migração macOS → Ubuntu Threadripper

> **Geração**: 2026-05-27 (Claude Opus 4.7 / 1M context)
> **Branch**: `feat/sprint-o1-quick-wins`
> **Commit base**: `d237d58 docs(v2.43): relatório Sprint O0/O1 T4 full benchmark — regressão analisada`
> **Objetivo**: Inventariar o sistema, mapear gaps e definir checklist para
> habilitar desenvolvimento local do Geosteering AI v2.0 nesta máquina.

---

## 0. TL;DR

| Dimensão | Estado | Bloqueio? |
|:---------|:-------|:----------|
| Hardware (CPU/RAM/GPU) | **Excelente** — Threadripper 7970X + 251 GiB + RTX A6000 48 GB | Não |
| Drivers NVIDIA / CUDA runtime | OK (driver 580, CUDA 13 visível para JAX/TF) | Não |
| `gfortran`, `gcc`, `make`, `cmake` | OK (GCC 13.3) | Não |
| `tatu.x` simulador Fortran | **Foi recompilado nesta sessão** (era Mach-O macOS) | **Resolvido** |
| Conda env `Geosteering_AI` (Python 3.13.13) | Existe, com TF 2.20 + JAX 0.7.2 GPU + Numba 0.65 | Parcial |
| Pacotes Python obrigatórios (`[dev]` + `[all]`) | **~15 faltando** (pytest, sklearn, matplotlib, fastapi, optuna, PySide6, …) | **SIM** |
| `geosteering-ai` em modo editável (`pip install -e .`) | **Não instalado** no env | **SIM** |
| Venv `~/Geosteering_AI_venv` (referência do CLAUDE.md) | **Não existe** — projeto usa conda env em vez disso | **Decisão** |
| Disco | **92% cheio** (152 GB livres / 1.9 TB) | **Atenção** |
| Hook `setup-environment.sh` (startup) | Roda mas chama `/usr/bin/python` 3.12 (sem pytest) | Cosmético |

**Resultado**: a máquina é altamente capaz; faltam apenas (a) instalar pacotes
faltantes no env, (b) decidir entre conda-env vs venv, (c) liberar disco antes
de qualquer treino sério.

---

## 1. Hardware Detectado

### 1.1 CPU

| Atributo | Valor |
|:---------|:------|
| Modelo | **AMD Ryzen Threadripper 7970X** (Zen 4 / Storm Peak) |
| Núcleos físicos / lógicos | **32 P / 64 L** (Hyper-Threading habilitado) |
| Clock máximo / mínimo | 5676 MHz / 410 MHz |
| Cache L1d/L1i/L2/L3 | 1 MiB / 1 MiB / 32 MiB / 128 MiB (4× CCX) |
| Extensões SIMD relevantes | AVX-512 (F/DQ/CD/BW/VL/IFMA/VBMI/VBMI2/VNNI/BITALG/VPOPCNTDQ/BF16), AVX2, AES-NI, SHA-NI, FMA |
| Recomendação `recommend_default_parallelism()` (v2.23) | `16w × 2t` (workers × threads) |

### 1.2 Memória

| Tipo | Total | Em uso | Livre | Buff/cache |
|:-----|:-----:|:------:|:-----:|:----------:|
| RAM  | **251 GiB** | 8.2 GiB | 204 GiB | 40 GiB |
| Swap | 8 GiB | 0 B | 8 GiB | — |

### 1.3 GPU

| Atributo | Valor |
|:---------|:------|
| Modelo | **NVIDIA RTX A6000** (GA102GL, Ampere) |
| VRAM | **48 140 MiB** (≈48 GB) |
| Driver | **580.159.03** |
| CUDA runtime exposto | **13.0** (driver-level) |
| Power | 31 W / 300 W idle |
| Devices `/dev/nvidia{0,ctl,modeset,uvm,uvm-tools,caps/*}` | Presentes |

### 1.4 Disco

| Mount | Tam. | Usado | Disp. | Uso |
|:------|:----:|:-----:|:-----:|:---:|
| `/` (NVMe) | 1.9 TB | 1.6 TB | **152 GB** | **92 %** |

> **Atenção**: 152 GB pode ser insuficiente para datasets P5/Picasso de grande
> porte (tensor histórico de simulação multi-freq × multi-ângulo pode chegar
> a 5+ GB por experimento). Sugere-se liberar ≥200 GB antes de iniciar
> treinamentos sérios — candidatos óbvios:
> `/home/daniel-leal/AntigravityProjects/Geosteering_AI/Geosteering_AI/.backups/`
> (34 subdiretórios) e `old_geosteering_ai/`.

---

## 2. Sistema Operacional e Toolchain

### 2.1 OS

| Atributo | Valor |
|:---------|:------|
| Distribuição | **Ubuntu 24.04.4 LTS** (Noble Numbat) |
| Kernel | `6.17.0-29-generic` |
| Arquitetura | x86_64 |
| Hostname | `rafaelstation` |

### 2.2 Compiladores e Build Tools

| Tool | Versão | Status |
|:-----|:------:|:------:|
| `gcc` | 13.3.0 | OK |
| `g++` | 13.3.0 | OK |
| `gfortran` | 13.3.0 | OK |
| `make` | 4.3 | OK |
| `cmake` | 3.28.3 | OK |
| `git` | 2.43.0 | OK |
| `gh` (GitHub CLI) | 2.45.0 | OK |
| `node` | 18.19.1 | OK |

### 2.3 CUDA / cuDNN

| Componente | Versão | Caminho | Status |
|:-----------|:------:|:--------|:------:|
| Driver NVIDIA | 580.159.03 | (kernel) | OK |
| CUDA visível ao driver | 13.0 | `nvidia-smi` | OK |
| `nvcc` (toolkit APT) | 12.0.140 | `/usr/lib/nvidia-cuda-toolkit/bin/nvcc` | OK (legacy) |
| CUDA Toolkit local | 12.4 (libs) | `/usr/local/cuda-12.4/` | OK |
| Symlink genérico | → cuda-12.4 | `/usr/local/cuda` | OK |
| **cuDNN** | — | (não detectado) | **Faltando se TF/JAX precisarem do legado** |
| `libnvblas.so.12` | 12.x | `/usr/local/cuda-12.4/targets/x86_64-linux/lib/` | OK |

> **Nota**: TF 2.20 e JAX 0.7.2 instalados via pip já trazem suas **próprias**
> stacks CUDA (via `jax-cuda12-pjrt`, `jax-cuda12-plugin`, e wheels TF).
> Não foi necessário cuDNN do sistema para detectar a A6000 com sucesso.

### 2.4 Bibliotecas científicas do sistema

`liblapack`, `libblas`, `libfftw`, `libhdf5_serial[_fortran]`, `libnetcdf` —
todas presentes em `/lib/x86_64-linux-gnu/`. Boa base para `scipy`,
`empymod` e bindings Fortran-Python (`f2py`).

---

## 3. Pythons Detectados

| Interpretador | Versão | Localização | Uso recomendado |
|:--------------|:------:|:------------|:----------------|
| `/usr/bin/python` | 3.12.3 | sistema (APT) | NÃO usar — bloqueado por PEP 668 |
| `/usr/bin/python3.12` | 3.12.3 | sistema | NÃO usar (idem) |
| `/home/daniel-leal/anaconda3/bin/python3.13` | 3.13.9 | conda **base** | NÃO usar |
| `/home/daniel-leal/anaconda3/envs/Geosteering_AI/bin/python` | **3.13.13** | **conda env** | ✅ **USAR ESTE** |

> **CLAUDE.md cita** `~/Geosteering_AI_venv` — esse venv **não existe** nesta
> máquina. O ambiente realmente provisionado é o conda env
> `Geosteering_AI`. **Decisão de governança requerida**: manter conda
> (status atual) ou criar o venv canônico recomendado em `CLAUDE.md`.
> Recomendação: **manter conda** (já tem 75 pacotes e GPU funcional) e
> atualizar `CLAUDE.md` para refletir conda; ou criar o venv para alinhar
> com a doc — ver §8 abaixo.

---

## 4. Pacotes Python no Conda Env `Geosteering_AI`

### 4.1 Inventário vs `pyproject.toml`

| Categoria | Pacote | Requerido | Instalado | Versão | Gap |
|:----------|:-------|:---------:|:---------:|:------:|:---:|
| **Core** | numpy | ≥1.24 | ✅ | 2.1.3 | — |
| Core | scipy | ≥1.10 | ✅ | 1.17.1 | — |
| Core | scikit-learn | ≥1.2 | ❌ | — | **FALTA** |
| Core | pyyaml | ≥6.0 | ✅ | 6.0.3 | — |
| **Train [train]** | tensorflow | ≥2.13 | ✅ | 2.20.0 | — |
| Train | keras | (via TF) | ✅ | 3.14.0 | — |
| **DL/JAX** | jax | — | ✅ | 0.7.2 | — |
| JAX | jaxlib | — | ✅ | 0.7.2 | — |
| JAX | jax-cuda12-pjrt | — | ✅ | 0.7.2 | — |
| JAX | jax-cuda12-plugin | — | ✅ | 0.7.2 | — |
| **Numba** | numba | — | ✅ | 0.65.1 | — |
| Numba | llvmlite | — | ✅ | 0.47.0 | — |
| Numba | numba-cuda | — | ✅ | 0.30.2 | (bônus) |
| **Viz [viz]** | matplotlib | ≥3.5 | ❌ | — | **FALTA** |
| **HPO [hpo]** | optuna | ≥3.0 | ❌ | — | **FALTA** |
| **API [api]** | fastapi | ≥0.110 | ❌ | — | **FALTA** |
| API | pydantic | ≥2.5 | ❌ | — | **FALTA** |
| API | uvicorn[standard] | ≥0.27 | ❌ | — | **FALTA** |
| **All extras** | joblib | ≥1.2 | ❌ | — | **FALTA** |
| All | pandas | ≥2.0 | ❌ | — | **FALTA** |
| All | tqdm | ≥4.65 | ❌ | — | **FALTA** |
| **Dev [dev]** | pytest | ≥7.0 | ❌ | — | **FALTA** (bloqueia testes) |
| Dev | pytest-cov | ≥4.0 | ❌ | — | **FALTA** |
| Dev | pytest-qt | ≥4.4 | ❌ | — | **FALTA** (bloqueia testes GUI) |
| Dev | mypy | ≥1.0 | ❌ | — | **FALTA** |
| Dev | watchdog | ≥3.0 | ❌ | — | **FALTA** |
| Dev | psutil | ≥5.9 | ❌ | — | **FALTA** |
| Dev | pre-commit | ≥3.0 | ❌ | — | **FALTA** |
| Dev | httpx | ≥0.27 | ❌ | — | **FALTA** |
| **GUI Qt** | PyQt6 | (Sim. Manager) | ❌ | — | **FALTA** (bloqueia GUI v2.7+) |
| GUI Qt | PySide6 | (Sim. Manager) | ❌ | — | **FALTA** |
| **Geofísica** | empymod | (paridade) | ❌ | — | **FALTA** (validação cruzada) |
| **Lint** | ruff | (pre-commit) | ❌ | — | **FALTA** |

> **Observação importante**: o conda env **tem TF + JAX + Numba GPU completos**
> mas **não tem o tooling de desenvolvimento**. Isso sugere que o env foi
> preparado prioritariamente para execução/treinamento e não para
> desenvolvimento iterativo.

### 4.2 Pacote `geosteering-ai` em modo editável

```bash
$ pip show geosteering-ai
# (vazio — NÃO instalado em modo editável)
```

Apesar disso, o import direto **funciona** porque o `sys.path` é
manipulado pelo pytest config (`pythonpath = ["."]`) e via `cd` no diretório
raiz:

```python
import geosteering_ai           # version 2.0.0 ✅
from geosteering_ai.config import PipelineConfig  # ✅
from geosteering_ai.simulation import simulate_multi  # ✅
from geosteering_ai.models import ModelRegistry  # ✅
from geosteering_ai.cli import main  # ✅
```

> **Mas**: os 3 entry-points CLI (`geosteering-cli`, `geosteering-warmup`,
> `geosteering-api`) NÃO estão disponíveis no PATH até instalar em modo
> editável.

### 4.3 Validação GPU

| Backend | Teste | Resultado |
|:--------|:------|:----------|
| TensorFlow 2.20 | `tf.config.list_physical_devices('GPU')` | `[PhysicalDevice(name='/physical_device:GPU:0', device_type='GPU')]` ✅ |
| TensorFlow 2.20 | `tf.test.is_built_with_cuda()` | `True` ✅ |
| JAX 0.7.2 | `jax.default_backend()` | `gpu` ✅ |
| JAX 0.7.2 | `jax.devices()` | `[CudaDevice(id=0)]` ✅ |
| JAX 0.7.2 | `xla_bridge.get_backend().platform` | `gpu` ✅ |

**A RTX A6000 está plenamente acessível pelos dois backends**. Não há
necessidade de instalar CUDA/cuDNN extras do sistema para uso atual.

---

## 5. Estado do Projeto

### 5.1 Estrutura

| Diretório | Tamanho | Comentário |
|:----------|:-------:|:-----------|
| `geosteering_ai/` | 47 MB (~167 .py) | Pacote v2.0 — OK |
| `tests/` | 12 MB (~83 testes) | Pasta presente |
| `docs/` | 5.4 MB | INDEX/ROADMAP/CHANGELOG/ARCHITECTURE OK |
| `Fortran_Gerador/` | 404 MB | inclui .out, .dat de validação |
| `sm_output/` | 2.4 MB | benchmarks gerados |
| `notebooks/` | 684 KB | colab templates inclusos |
| `legacy/` | 332 KB | código C0-C47 referência |
| `.backups/` | (34 subdirs) | **candidato a limpeza para liberar disco** |
| `old_geosteering_ai/` | — | **candidato a remoção (já migrado)** |

### 5.2 Estado do Git

| Item | Valor |
|:-----|:------|
| Branch atual | `feat/sprint-o1-quick-wins` |
| Commits desde main | (ver `git log main..HEAD`) |
| Arquivos modificados | 6 |
| Arquivos não-rastreados | 11 |
| Arquivos deletados (uncommitted) | 4 (3 sprints arquivados com nome normalizado) |
| Arquivos novos importantes | 5 reports v2.43 (GPU analysis, reflexão modular, reformulação JAX, sprint O0/O1) |

### 5.3 Hooks Claude Code Configurados

| Evento | Hooks ativos | Risco no novo OS |
|:-------|:-------------|:-----------------|
| `SessionStart:startup` | `setup-environment.sh` | Usa `${PYTHON:-python}` que cai em `/usr/bin/python` 3.12 sem pytest → log "No module named pytest" no startup. **Cosmético, não bloqueia.** |
| `SessionStart:compact` | `reinject-errata.sh` | Sem impacto |
| `PreToolUse:Edit\|Write` | 7 hooks (backup, lock, anti-patterns, physics, no-pytorch, protect, version-refs) | OK |
| `PreToolUse:Bash` | `colab-token-refresh.sh` | OK |
| `PostToolUse:Edit\|Write` | 7 hooks (lock release, compile-check, lint, autoformat, sci-refs, fortran-parity, ptbr) | `run-fortran-parity.sh` agora funciona pois `tatu.x` é Linux ELF ✅ |
| `Stop` | `release-lock.sh --all`, `run-pytest.sh` | `run-pytest.sh` falhará silenciosamente sem pytest |

### 5.4 Sprint em Execução

| Item | Valor |
|:-----|:------|
| Sprint atual | **O0 (Pre-Flight JAX GPU Optimization)** → já avançou para **O1 (Quick Wins)** |
| Plano | `docs/reports/v2.43_jax_gpu_optimization_plan.md` Parte III |
| Branch | `feat/sprint-o1-quick-wins` |
| Backlog ativo (P1) | `A-jax-gpu-dispatcher`, `A-jax-gpu-data-gen`, `C-surrogate-train` (bloqueado em A) |
| Próximas sprints | O2 (chunking + async), O3 (memory layout), O4 (complex64 opt-in) |
| Snapshot mais recente | `docs/sprints/v2.43.md` (notebook reescrito + 8 bugs fix metodológicos) |

---

## 6. Ações Já Executadas Nesta Auditoria

| # | Ação | Resultado |
|:-:|:-----|:----------|
| 1 | Backup de `Fortran_Gerador/tatu.x` (Mach-O macOS) | `tatu.x.macos.bak` (564 KB) |
| 2 | `make clean && make` em `Fortran_Gerador/` | ✅ **`tatu.x` agora é ELF 64-bit Linux** (637 KB, vinculado a libgfortran 5, libgomp, glibc) |
| 3 | Validação import `geosteering_ai` | ✅ OK (config, simulation, cli, models) |
| 4 | Validação GPU TF | ✅ A6000 detectada |
| 5 | Validação GPU JAX | ✅ CudaDevice(id=0) |
| 6 | Detecção de gaps de pacotes Python | 15+ pacotes faltantes catalogados |

---

## 7. Gaps Bloqueantes (precisa resolver antes de desenvolver)

| # | Gap | Severidade | Esforço | Ação proposta |
|:-:|:----|:----------:|:-------:|:--------------|
| G1 | `pytest`, `pytest-cov`, `pytest-qt`, `mypy`, `ruff` ausentes | **ALTO** | 5 min | `pip install -e ".[dev]"` no env |
| G2 | `tensorflow`, `keras` OK; mas `matplotlib`, `optuna`, `joblib`, `pandas`, `tqdm` ausentes | **ALTO** | 5 min | `pip install -e ".[all]"` no env (cobre G1+G2) |
| G3 | `scikit-learn` ausente (dependência core do pyproject) | **ALTO** | (incluído em G2) | idem |
| G4 | `pre-commit`, `watchdog`, `psutil`, `httpx` ausentes | **MÉDIO** | (incluído em G2) | idem |
| G5 | `PyQt6` / `PySide6` ausentes — Simulation Manager GUI não roda | **MÉDIO** | (incluído em G2 via `pytest-qt`?) | Instalar explicitamente: `pip install PyQt6 PySide6` |
| G6 | `empymod` ausente (paridade EM cruzada) | **BAIXO** | 1 min | `pip install empymod` |
| G7 | `geosteering-ai` não instalado em modo editável | **MÉDIO** | 1 min | `pip install -e .` (entry-points CLI passam a funcionar) |
| G8 | `~/Geosteering_AI_venv` não existe (CLAUDE.md diverge) | **DECISÃO** | 15 min ou 5 min | Ver §8 |
| G9 | Disco em 92% (152 GB livres) | **ATENÇÃO** | 30 min | Limpar `.backups/`, `old_geosteering_ai/`, `*.zip` na raiz |
| G10 | Hook `setup-environment.sh` chama `python` do sistema | **COSMÉTICO** | 5 min | Setar `PYTHON=/home/.../envs/Geosteering_AI/bin/python` em `~/.bashrc` ou alterar o hook |
| G11 | `tatu.x` era macOS | **RESOLVIDO nesta sessão** | — | Recompilado ✅ |

---

## 8. Decisão Aberta: Conda vs Venv

| Critério | Manter conda `Geosteering_AI` | Criar venv `~/Geosteering_AI_venv` |
|:---------|:------------------------------|:-----------------------------------|
| Alinha com CLAUDE.md | ❌ (diverge) | ✅ |
| Pacotes já instalados | ✅ 75 pacotes (TF + JAX-GPU + Numba) | ❌ Tudo do zero |
| Tempo para usar | 5 min (instalar 15 gaps) | 20 min (criar venv + reinstalar tudo + recompilar JAX wheels CUDA) |
| Risco de regressão | Baixo (stack já validado para GPU) | Médio (precisa re-validar JAX GPU + TF GPU) |
| Aderência ao padrão do projeto | Histórico mostra venv no CLAUDE.md | — |
| Manutenção long-term | Conda gerencia CUDA libs auto | venv requer pip puro |

**Recomendação**: manter conda env e **atualizar `CLAUDE.md`** seção "Ambiente
Python" para refletir a realidade desta máquina (entrada adicional Linux/
Ubuntu/conda), preservando a recomendação venv para macOS/dev anteriores.

---

## 9. Próximos Passos (Checklist Executável)

### 9.1 Habilitação imediata do ambiente (≈10 min)

```bash
# 0. Ativar env
source /home/daniel-leal/anaconda3/etc/profile.d/conda.sh
conda activate Geosteering_AI

# 1. Instalar projeto em modo editável + dev + all
cd /home/daniel-leal/AntigravityProjects/Geosteering_AI/Geosteering_AI
pip install -e ".[dev,all]"

# 2. Pacotes GUI (não estão em [dev,all] do pyproject)
pip install PyQt6 PySide6

# 3. Pacote opcional de paridade EM
pip install empymod

# 4. Pre-commit (instala hooks Git locais)
pre-commit install

# 5. Verificar instalação
geosteering-cli version
geosteering-warmup --help
pytest --collect-only -q | tail -3
```

### 9.2 Validação smoke (≈5 min após 9.1)

```bash
# Suite mais leve (config + factory)
pytest tests/test_config.py tests/test_models.py -q --tb=short

# Paridade Fortran <1e-12 (sagrada) — agora possível com tatu.x Linux
pytest tests/ -k "fortran_parity" -q --tb=short

# Smoke GPU JAX (109 testes marker gpu)
pytest tests/ -m "gpu" -q --tb=short
```

### 9.3 Validação benchmark (≈5 min)

```bash
# Warmup JIT (esquenta caches Numba + LLVM)
geosteering-warmup --verbose

# Cenário E baseline (deve dar >90k mod/h em Threadripper)
geosteering-cli benchmark --scenario E --n 200

# Cenário A baseline (deve dar >1M mod/h)
geosteering-cli benchmark --scenario A --n 200
```

### 9.4 Liberação de disco (≈30 min, opcional mas recomendado)

```bash
# Inspeção: maiores diretórios em .backups
du -sh .backups/*/ | sort -h | tail -10

# Após confirmar com você quais backups remover:
# rm -rf .backups/<old-snapshot>
# rm -rf old_geosteering_ai/
# rm -f latex-document-skill-main.zip   # 18 MB na raiz
```

### 9.5 Sincronização CLAUDE.md (≈10 min)

Atualizar seção "Ambiente Python" do `CLAUDE.md` adicionando uma linha
sobre o conda env Linux:

```diff
 | Contexto | Versão | Ambiente |
 | :------- | :----- | :------- |
-| Desenvolvimento local | Python 3.13 | `~/Geosteering_AI_venv` |
+| Desenvolvimento local (macOS) | Python 3.13 | `~/Geosteering_AI_venv` |
+| Desenvolvimento local (Linux Threadripper) | Python 3.13 | `conda env Geosteering_AI` |
 | CI/CD GitHub Actions | Python 3.13 (primária), 3.12 (fallback) | ubuntu-latest |
```

### 9.6 Sprint em andamento

Após 9.1–9.3 verdes, o trabalho da **Sprint O1 (Quick Wins JAX GPU)** pode
continuar. Diferenciais desta máquina vs Colab T4 usada na baseline:

| Aspecto | Colab T4 (baseline atual) | Esta máquina (RTX A6000) |
|:--------|:-------------------------|:------------------------|
| VRAM | 16 GB | **48 GB** (3×) |
| FP32 TFLOPs | ~8.1 | **~38.7** (4.8×) |
| FP64 TFLOPs | ~0.25 | **~0.60** (2.4×) |
| Tensor Cores | gen 2 | **gen 3** (BF16/TF32 nativo) |
| CPU host | 2 vCPU shared | **32 P / 64 L Threadripper** |
| RAM host | 12 GB | **251 GB** (20×) |

**Implicação**: a otimização O1-O4 pode ser desenvolvida e validada
localmente sem depender de Colab para iterações rápidas. Reservar Colab
apenas para validação final em A100 (target de referência).

---

## 10. Resumo do que NÃO foi feito

- ❌ Não foi executado `pip install` (requer confirmação do usuário sobre
  conda vs venv).
- ❌ Não foi executado `pytest` (bloqueado por G1).
- ❌ Não foram removidos `.backups/` (requer confirmação por destruir
  histórico).
- ❌ Não foi atualizado `CLAUDE.md` (requer aprovação da decisão §8).
- ❌ Não foi rodado benchmark Cenário E na A6000 (depende de G1+G2).

---

## 11. Referências

- **CLAUDE.md** §"Ambiente Python (Obrigatório)" — venv esperado
- **`pyproject.toml`** — fonte de verdade de dependências `[dev]` e `[all]`
- **`docs/sprints/CURRENT.md`** — Sprint O0 / O1 em execução
- **`docs/ROADMAP.md`** §0 — Backlog priorizado (SSoT)
- **`Fortran_Gerador/Makefile`** — alvo `make` para Linux (sem
  `LDFLAGS_EXTRA`), `make debug_O0` para validação numérica
- **`.claude/hooks/setup-environment.sh`** — script de inicialização da
  sessão (chama `${PYTHON:-python}`)

---

*Relatório de auditoria de ambiente — formato customizado (não segue
`report_template.md` pois este é uma auditoria de infraestrutura, não
relatório pós-sprint). Aderente à regra "≥70% dados estruturados" do
CLAUDE.md.*
