# Relatório v2.59 — SM MVVM Turn 7: perfis completos · defaults do monólito · fontes +1pt · JAX VRAM/chunk · 6g export

| Campo | Valor |
|:------|:------|
| **Data** | 2026-06-25 |
| **Autor** | Daniel Leal (impl. Claude Opus 4.8 1M · Ultracode/Dynamic Workflow) |
| **Escopo** | SM MVVM (`apps/sim_manager/`) + fundação GUI (`geosteering_ai/gui/`) |
| **Entrega** | 4 PRs empilhados sobre #63 — **#64 · #65 · #66 · #67** |
| **Restrição** | Física/paridade Fortran <1e-12 **intocada**; TLS-safety preservada; CLI/libs/Studio sem regressão |
| **Método** | Investigação (workflow 6 agentes) → Plan Mode → impl. por PR → revisão adversarial por PR → fix → commit |

---

## 1. Pedido (Turn 7) → Entrega

| # | Pedido do usuário | PR | Status |
|:-:|:------------------|:--:|:------:|
| 1 | Incluir TODAS as configs dos perfis pré-configurados (não só Oklahoma 3), ref. monólito | #64 | ✅ |
| 2 | Aumentar +1pt as fontes do programa | #65 | ✅ |
| 3 | Defaults de simulação do MVVM == SM monólito | #64 | ✅ |
| 4c | Ordem de env de pré-alocação de VRAM no subprocesso | #66 | ✅ |
| 4d | `jax_chunk_size_models` fixo | #66 | ✅ |
| 4 / 6g | Plot composer | #67 | ✅ (escopo estreito: export) |
| 4 / 6f | Benchmark entre simuladores | — | ⏸️ adiado (pedido do usuário) |

---

## 2. Achado-chave da investigação (corrige o enunciado ingênuo)

| Tema | Premissa ingênua | Ground truth (workflow 6 agentes) |
|:-----|:-----------------|:----------------------------------|
| Perfis | "Só Oklahoma 3 importa" | O combo JÁ lista os 7 e a **geologia JÁ importa p/ todos**. O gap real: o MVVM não espelhava os campos DERIVADOS do estocástico (ρ/λ/min_thick/n_models). Oklahoma 3/5 "pareciam completos" só porque sua freq/TR/dip == defaults. |
| Perfis | "Importar freq/TR/dip do perfil" | **Nem** monólito **nem** MVVM fazem isso (são params de ferramenta, preservados). Espelhar o monólito = só os campos derivados. |
| 6g | "Plot composer = grande diálogo modal" | A grade multi-painel já é entregue pela galeria **6d**. A única lacuna real é **export + DPI**. |
| 4c | "Setar env no subprocesso" | A ordem JÁ está correta (`_jax/__init__::_setup_xla_environment` roda antes de `import jax`). Entrega = `initializer` defense-in-depth + guard. |
| Slices | "CURRENT.md diz fazer 6e" | **STALE**: git mostra 6e/6h/6i já mergeados. Restavam só 6f (adiado) + 6g. |

---

## 3. PRs

### PR #64 — `feat/sm-config-fidelity` (itens 1+3) — risco MÉDIO
| Mudança | Arquivo | Detalhe |
|:--------|:--------|:--------|
| Espelha campos derivados ao aplicar perfil | `…/simulation/viewmodel.py:apply_canonical_profile` | ρ_h_min/max, min_thickness=max(1e-3,min(esp)), anisotropic (tol 1e-9), λ=√(max(ρv/ρh,1)) min/max, n_layers_min=n / max=n+1 (excl.), n_models=1 — espelha `simulation_manager.py:1974-2057` |
| View força auto-geo ON ao aplicar | `…/simulation/view.py:_on_apply_canonical` | espelha `sim_manager:2014-15` (default de produção é OFF) |
| Defaults == produção do monólito | `…/simulation/viewmodel.py:__init__` | h1=10, tj=120, p_med=0.2 (⇒ n_pos=600), n_models=2000, min_thick=0.5, rho_h_max=1800, n_layers_max=32 (excl.), n_layers_fixed=None, save_artifacts=True, h1/tj_auto=False |
| Spinbox λ a 4 casas | `…/simulation/view.py` | √2→1.4142 (paridade monólito) |

**Errata intocada:** freq=20000, TR=1.0, dip=0 já coincidiam. **Testes:** 3 asserts atualizados + e2e fixa params pequenos + 3 novos (`test_sm_lote2`). **386/386 SM/GUI PASS.**

### PR #65 — `feat/sm-font-bump` (item 2) — risco BAIXO
`ThemeTokens.font_size_{sm,base,lg}` `12/14/18 → 13/15/19` (knob único → `generate_qss` → `apply_theme`); `activity_bar` glyph `15→16 pt`. Golden atualizado. **27/27 PASS.** Pura apresentação.

### PR #66 — `feat/sm-jax-vram-chunk` (4c+4d) — risco BAIXO/MÉDIO
| Op | Mudança | Detalhe |
|:--|:--------|:--------|
| c | `initializer=_jax_pool_initializer` no pool persistente | `os.environ.setdefault("XLA_PYTHON_CLIENT_PREALLOCATE","false")` no filho, pré-import; **vive em `sim_request` (Qt-FREE)** p/ o filho não importar Qt (fix da revisão) |
| c | guard test | nenhum `import jax` de topo fora de `_jax/` |
| d | `SimRequest.jax_chunk_size_models=64` + 2 call-sites | chunka o eixo de modelos → limita VRAM (torna n_models=2000 executável na A6000); só backend JAX |

**Paridade chunk:** <1e-13 vs chunk=None (GPU bit-exato; CPU ~1e-14 ULP) — `test_o4_chunk_size_models_invariante`. **9 testes novos; 85/85 JAX/SM PASS.**

### PR #67 — `feat/sm-plot-export-6g` (6g) — risco MÉDIO
Botão "Exportar figura…" + DPI (50-600, default 150) na `ResultsView`. `_save_current_figure` renderiza o estado atual num `MatplotlibCanvas` **off-screen** (replay de `_draw_current_mode`, caminho único extraído de `_render`) → salva honrando DPI/PDF/SVG/EPS **independente do backend on-screen**; restaura o canvas + teardown no `finally`. ViewModel Qt-free; sem re-simulação. **15 testes; 61/61 PASS.**

---

## 4. Revisões adversariais (Workflow por PR)

| PR | Findings | Resolução |
|:--:|:---------|:----------|
| #64 | 6 (nit/minor) | λ→4 casas + comentário "numba default" corrigidos; demais = paridade monólito intencional (collapse de knobs, run pesado por default) |
| #66 | 5 (1 REFUTED) | **initializer movido p/ módulo Qt-free** (filho não puxa Qt); comentários "bit-idêntico" suavizados (<1e-13/CPU ULP) |
| #67 | 5 (1 REFUTED) | **normaliza extensão** pelo filtro (evita PNG silencioso); **gate do botão == guard** (has_result E depth); **descarta canvas off-screen** no finally |

Nenhum **blocker/major**. Todos os achados CONFIRMED acionáveis foram corrigidos; os PLAUSIBLE/nit remanescentes são latentes (gated em edição futura) ou paridade intencional.

---

## 5. Validação

| Gate | Resultado |
|:-----|:----------|
| `ruff` + `ruff-format` + `pre-commit` (mypy gated `geosteering_ai`) | ✅ limpo (todos os PRs) |
| Suíte cumulativa SM/GUI/JAX (`JAX_PLATFORMS=cpu xvfb-run`) | **424 passed, 5 skipped** |
| Paridade chunk JAX <1e-13 | ✅ `test_o4_chunk_size_models_invariante` |
| `test_qt_binding_boundary` + `test_sm_app_boot` | ✅ |
| Física do kernel / `simulate_batch` / `positions_z` / `compute_n_pos` | ✅ **intocados** |
| `geosteering-warmup --jax` + CLI | ✅ `test_cli_warmup_jax` 11/11 (alinhamento chunk=64) |

**Pendente (empírico, requer A6000):** rodar SM JAX com os novos defaults (2000 modelos) confirmando que chunk=64 evita OOM e o resultado é bit-idêntico a chunk=None.

---

## 6. Riscos conscientes (documentados, não-bugs)

1. **1ª execução pesada por design** — defaults agora são os de produção do monólito (2000 modelos × n_pos 600). Fiel ao monólito (que também não confirma). Smoke-tests usam params pequenos explícitos.
2. **Aplicar perfil colapsa knobs estocásticos** (n_models=1, range→{n}) e não desfaz ao voltar p/ "Aleatória" — paridade exata com o monólito.
3. **chunk=64**: fatia-cauda (<64) ou n_models≠64 forçado paga 1 compile XLA único não-aquecido (perf/cold-start, documentado).

---

## 7. Estado do GitHub & próximos passos

> ⚠️ **Stack profunda:** #56→#63 (8 PRs, Turns anteriores) + **#64→#67 (este Turn)** = **12 PRs abertos, nenhum mergeado.** Recomendação forte: **mergear #56…#63 na `main` antes de prosseguir** (reduz risco de conflito/drift e desbloqueia o CI da main).

**Pós-merge (lembrete):** atualizar `docs/sprints/CURRENT.md` (STALE) — 6e/6h/6i DONE; restam 6f (adiado) + 6g (#67).
